# --- Dependências Principais ---
pandas
openpyxl        # Necessário para o Pandas salvar arquivos .xlsx
PyYAML          # Para ler o arquivo de configuração config.yaml
python-dotenv   # Para ler o arquivo .env com a chave da API

# --- Dependência da IA ---
openai

# --- Dependências para Formatos de Arquivo e Funções Específicas ---
python-dateutil # Para parsing de datas de forma flexível
ofxparse        # Para ler arquivos de extrato .ofx/.ofc
pdfplumber      # Para ler texto e dados de arquivos PDF
camelot-py[cv]  # Para extrair tabelas de PDFs (o [cv] inclui dependências de visão computacional)
Pillow          # Para manipulação de imagens (usado por pdfplumber/camelot)
pytesseract     # Para OCR (reconhecimento de texto) em PDFs escaneados